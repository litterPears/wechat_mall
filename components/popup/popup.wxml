<!--components/popup/popup.wxml-->
<view class="popup" wx:if="{{popShow || show}}">
    <view class="popupMian">
        <text class="tipsDom tipsback"></text>
        <text class="popuptips tipsback">{{popText}}</text>
    </view>
</view>
